DEBUG=1
HOST=localhost
DATABASE_URL=postgres://marky:marky@db:5432/marky
SECRET_KEY=docker-local-dev-secret-key-change-me
FRONTEND_URL=http://localhost:3000

# Email — leave blank to skip sending in local Docker testing
EMAIL_HOST=smtp.resend.com
EMAIL_HOST_USER=resend
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=no-reply@example.com
EMAIL_PORT=587

# R2 — leave blank to store media in local container volume instead
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_STORAGE_BUCKET_NAME=
AWS_S3_ENDPOINT_URL=
AWS_S3_CUSTOM_DOMAIN=

LOGGING_LEVEL=DEBUG
